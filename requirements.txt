# QuantML Core Dependencies
# Core library works without NumPy, but NumPy is recommended for performance

# Optional but recommended
numpy>=1.20.0,<2.0.0

# Configuration (optional, for YAML support)
pyyaml>=6.0,<7.0

# Data processing (optional, for advanced features)
pandas>=1.3.0,<3.0.0

